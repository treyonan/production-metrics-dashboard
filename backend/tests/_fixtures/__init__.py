"""Test-only support modules.

Anything under ``tests/_fixtures/`` is test infrastructure, not
production code. Production code under ``backend/app/`` MUST NOT
import from this package -- the wall is one-directional.
"""
